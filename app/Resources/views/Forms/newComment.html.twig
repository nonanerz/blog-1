 <h1>New Comment</h1>
        {{ form_start(commentType, { 'action': path('new_comment' , { 'id' :id }), 'method': 'POST', 'attr': {'class': 'main'} }) }}
        {{ form_widget(commentType) }}
        <button type="submit" class="btn btn-primary">Сохранить</button>
        {{ form_end(commentType) }}
