{% extends 'base.html.twig' %}

{% block main %}
<div class="col-lg-12 main">
    <table class="table table-hover">
        <tr>
            <th>Name</th>
            <th>Registred</th>
            <th>Role</th>
        </tr>

        {% for user in users %}
            <tr>
                <td>{{ }}</td>
                <td>{{ }}</td>
                <td>{{ }}</td>
            </tr>
        {% endfor %}

    </table>
</div>
{% endblock %}
